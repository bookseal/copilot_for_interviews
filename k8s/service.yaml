apiVersion: v1
kind: Service
metadata:
  name: signal-copilot
  namespace: signal-copilot
  labels:
    app: signal-copilot
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: signal-copilot
